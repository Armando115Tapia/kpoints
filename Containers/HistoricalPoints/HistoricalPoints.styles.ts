import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    getStartedText: {
        fontSize: 17,
        lineHeight: 24,
        textAlign: 'center',
    },
    title: {
        fontSize: 20,
        fontWeight: 'bold',
    }
});
