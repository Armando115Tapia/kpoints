import { StyleSheet } from "react-native";

export const RedeemItemStyles = StyleSheet.create({
  container: {
    justifyContent: "space-between",
    flexDirection: "row",
    backgroundColor: "transparent",
    marginVertical: 15,
  },
});
